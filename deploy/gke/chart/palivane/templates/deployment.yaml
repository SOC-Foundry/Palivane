apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "palivane.name" . }}
  labels:
    {{- include "palivane.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        {{- include "palivane.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "palivane.name" . }}
      {{- if .Values.nodePool.isolate }}
      # Land only on this customer's dedicated node pool (Terraform taints + labels it).
      nodeSelector:
        palivane-customer: {{ .Values.customer | quote }}
      tolerations:
        - key: dedicated
          operator: Equal
          value: {{ .Values.customer | quote }}
          effect: NoSchedule
      {{- end }}
      initContainers:
        # Cloud SQL Auth Proxy as a NATIVE sidecar (starts before, runs alongside the app).
        - name: cloud-sql-proxy
          image: {{ .Values.cloudsql.proxyImage }}
          restartPolicy: Always
          args:
            - "--structured-logs"
            - "--port=5432"
            - {{ .Values.cloudsql.connectionName | quote }}
          securityContext:
            runAsNonRoot: true
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
      containers:
        - name: palivane
          image: "{{ .Values.image.repo }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.port }}
          env:
            - name: PORT
              value: {{ .Values.port | quote }}
            - name: PALIVANE_PUBLIC_URL
              value: "https://{{ .Values.host }}"
            - name: CORS_ORIGINS
              value: "https://{{ .Values.host }}"
            - name: PALIVANE_ALLOWED_HOSTS
              value: {{ .Values.host | quote }}
            - name: GATEWAY_ENFORCE
              value: {{ .Values.config.gatewayEnforce | quote }}
            - name: PALIVANE_ALLOW_SIGNUP
              value: {{ .Values.config.allowSignup | quote }}
            - name: PALIVANE_ENCRYPT_FINDINGS
              value: {{ .Values.config.encryptFindings | quote }}
            - name: PALIVANE_STORE_CONTENT
              value: {{ .Values.config.storeContent | quote }}
            - name: PALIVANE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "palivane.secretName" . }}
                  key: PALIVANE_SECRET_KEY
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "palivane.secretName" . }}
                  key: DATABASE_URL
          readinessProbe:
            httpGet: { path: /api/health, port: {{ .Values.port }} }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/health, port: {{ .Values.port }} }
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
