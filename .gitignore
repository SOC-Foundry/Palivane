# Python
backend/.venv/
__pycache__/
*.pyc
*.db
backend/_*.db

# Secrets & local env (commit the *.example templates, never the real .env)
.env
backend/.env
*.pem

# Node / build artifacts
frontend/node_modules/
frontend/dist/
extension/*.zip

# Misc
.DS_Store

# Terraform
deploy/terraform/.terraform/
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/gke/terraform/.terraform/
deploy/gke/terraform/*.tfstate
deploy/gke/terraform/*.tfstate.*
deploy/terraform/*.tfvars
deploy/terraform/.terraform.lock.hcl
!deploy/terraform/terraform.tfvars.example

# Native release tarballs
/dist/

# hero-video rig deps (not part of the product)
scripts/hero-video/node_modules/

# Local no-Docker dev rig (see RUNNING-LOCALLY.md)
.env.local
/palivane-local.db

# Rendered demo soundtracks — regenerable intermediates (only the muxed .mp4 is tracked)
marketing/*-score.wav
