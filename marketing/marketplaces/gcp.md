# Google Cloud Marketplace — self-hosted listing via Producer Portal

We already run on GCP, so this doubles as a partner-relationship start.

1. Partner Advantage signup (free tier) with the SOC Foundry org →
   console.cloud.google.com/producer-portal under the `palivane` project's org.
2. Create product → **Kubernetes/VM/SaaS**: start with the container/K8s listing of the
   self-hosted edition (the compose stack; a minimal Helm wrapper is the one eng artifact
   this needs — track as follow-up).
3. Paste listing copy; logo `assets/logo-512.png`.
4. Transactable SaaS later needs the Cloud Marketplace Procurement API integration.
