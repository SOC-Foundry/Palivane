fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0.36
pydantic>=2.10
anthropic>=0.40         # LLM judge — Claude backend
openai>=1.50            # LLM judge — GPT backend (optional)
google-genai>=0.3       # LLM judge — Gemini backend (optional)
alembic>=1.13           # schema migrations
psycopg2-binary>=2.9    # PostgreSQL driver
cryptography>=42        # encrypt per-tenant upstream provider keys at rest
argon2-cffi>=23         # argon2id password hashing (legacy PBKDF2 still verified)
authlib>=1.3.1,<2       # OIDC ID-token / JWKS validation for SSO (>=1.3.1: CVE-2024-37568 JWS alg confusion)
prometheus-client>=0.20 # /metrics exposition
python3-saml>=1.16      # per-tenant SAML SSO (pulls lxml + xmlsec manylinux wheels)
python-multipart>=0.0.18 # SAML ACS form parsing (>=0.0.18: CVE-2024-53981 multipart DoS)
boto3>=1.34          # optional SIEM/data-lake delivery to S3 (per-tenant, lazy-imported)

# --- dev / test ---
pytest>=8.0
httpx>=0.27          # FastAPI TestClient
