{
  "agent": "claude-code",
  "captured_from": "synthetic — the drift case, not a capture",
  "note": "UserPromptSubmit with the prompt field renamed. This is the failure the whole mechanism exists for: the hook fires, returns 0, and the heartbeat stays green while nothing is scanned.",
  "event": {"hook_event_name": "UserPromptSubmit", "cwd": "/tmp/proj",
            "user_message": "refactor the billing module and push"},
  "expect": {"kind": "parse_miss"}
}
